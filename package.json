{
  "name": "roofy-website-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the Roofy Investments static site (Tailwind compile + prerender).",
  "scripts": {
    "prerender": "node prerender.js",
    "css": "npx -y tailwindcss@3.4.17 -c tailwind.config.js -i tailwind.input.css -o site/assets/css/tailwind.css --minify",
    "build": "npm run css && npm run prerender"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
