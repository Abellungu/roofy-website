{
    "name": "roofy-admin",
    "version": "1.0.0",
    "private": true,
    "description": "Self-hosted bilingual admin panel for the ROOFY static site. No database: JSON files are the store, git is the history.",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "create-user": "node scripts/create-user.js",
        "test": "node --test tests/*.test.js"
    },
    "engines": { "node": ">=18" },
    "dependencies": {
        "express": "^4.19.2",
        "multer": "^2.0.1",
        "sharp": "^0.33.4"
    }
}
